// Config grabber (downleveling)

const constants = require("../../config/constants");
const errors = require("../../config/errors");
const defaults = require("../../config/defaults");

module.exports = {
  constants,
  errors,
  defaults,
};
